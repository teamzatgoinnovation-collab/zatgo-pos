# ZatGo Electron desktop — copy to .env (Vite loads VITE_* for renderer)
# Secrets must not be embedded in the renderer bundle.
# ERPNext sessions stay in the Electron main process (memory).

# Optional default site URL for the connection screen
VITE_FRAPPE_BASE_URL=http://127.0.0.1:8082
